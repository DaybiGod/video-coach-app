# Diagnostico temporal: aisla si el problema es de cuenta (Actions) o especifico de macOS.
name: ping
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/ci-ping.yml
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions funciona en esta cuenta"
